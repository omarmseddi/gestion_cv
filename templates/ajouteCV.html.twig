{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Ajouter un CV</h1>
                {{ form_start(CVForm) }}
                {{ form_widget(CVForm) }}
                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(CVForm) }}
            </div>
        </div>
    </div>
{% endblock %}